name: AppSecAI Security Analysis — All Pending

on:
  workflow_dispatch:

jobs:
  submit-all:
    uses: ./.github/workflows/appsecai-scan.yml
    with:
      cve_id: ''
    permissions:
      contents: write
      id-token: write
